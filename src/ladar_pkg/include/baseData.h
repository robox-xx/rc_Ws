#ifndef _baseData_
#define _baseData_
#include <math.h>
#define PI acos(-1)

const int DATA_NUM = 360; //雷达数据个数
const float R = 1 / 2 ; // 壶圆柱半径
const float ANGLE_INCREMENT = PI/360;  // 雷达角度增量(3 * PI / 2) / 360
#endif

